---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-gb', {
			year: 'numeric',
			month: 'numeric',
      day: 'numeric',
		})
	}
</time>
