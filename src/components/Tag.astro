---
interface Props {
  tag: string;
  linked?: boolean;
}

const { tag, linked = true } = Astro.props;
---

{
  linked ? (
    <a
      href={`/blog/tag/${tag}`}
      class="px-3 py-1 border border-card text-sm text-fg-lighter hover:border-special hover-special"
    >
      #{tag}
    </a>
  ) : (
    <span class="text-special">#{tag}</span>
  )
}
